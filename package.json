{
  "name": "historical-knowledge",
  "version": "1.0.0",
  "scripts": {
    "build": "npx -p @tailwindcss/cli tailwindcss -i ./input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  }
}
