{
  "name": "historical-knowledge",
  "version": "1.0.0",
  "scripts": {
    "build": "node ./node_modules/.bin/tailwindcss -i ./input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^4.2.2"
  }
}
